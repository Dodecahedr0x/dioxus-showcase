// Auto-generated by dioxus-showcase. Do not edit by hand.
use dioxus::prelude::*;
use std::collections::HashSet;

pub type ComponentRenderFn = Box<dyn Fn() -> Element>;
pub const SHOWCASE_GENERATION: &str = "{{generation}}";

pub struct ShowcaseComponentDefinition {
    pub id: String,
    pub title: String,
    pub tags: Vec<String>,
    pub render: ComponentRenderFn,
}

pub fn showcase_components() -> Vec<ShowcaseComponentDefinition> {
    let mut components = Vec::new();
    let mut ids = HashSet::new();
{{#each components}}
    for story in {{story_path}}(r#"{{source_path}}"#, r#"{{module_path}}"#) {
        let story_id = story.definition.id.clone();
        assert!(ids.insert(story_id.clone()), "duplicate showcase id '{}'", story_id);
        components.push(ShowcaseComponentDefinition {
            id: story.definition.id,
            title: story.definition.title,
            tags: story.definition.tags,
            render: story.render,
        });
    }
{{/each}}
    components
}
