[package]
name = "dioxus-showcase"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Facade crate for building storybook-style Dioxus showcases."
keywords = ["dioxus", "showcase", "storybook", "components", "ui"]
categories = ["gui", "wasm", "web-programming", "development-tools"]

[lib]
name = "dioxus_showcase"
path = "src/lib.rs"

[dependencies]
dioxus = { workspace = true }
dioxus-showcase-core = { workspace = true }
dioxus-showcase-macros = { workspace = true }
# Link-time story/provider registration. The `registration` module that uses it
# lands with the registration node; declared here so the contract and the
# lockfile are settled first.
inventory = { workspace = true }

[lints]
workspace = true
